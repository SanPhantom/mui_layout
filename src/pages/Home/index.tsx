import * as React from 'react';

export interface HomeProps {
  // props
}

const Home: React.SFC<HomeProps> = props => {
  // const { props } = props;
  return (
    <span>Body</span>
  );
}

export default Home;