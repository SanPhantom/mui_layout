import * as React from 'react';

export interface AboutProps {
  // props
}

const About: React.SFC<AboutProps> = props => {
  // const { props } = props;
  return (
    <span>About</span>
  );
}

export default About;